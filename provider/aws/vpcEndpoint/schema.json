{
  "resources": {
    "anvil:aws:VpcEndpoint": {
      "isComponent": true,
      "description": "An Anvil-managed AWS Interface VPC Endpoint. Creates one ENI per private subnet with private DNS enabled. The endpoint security group uses a self-referencing ingress rule on port 443 — only compute resources that have been explicitly granted access can reach the endpoint at the network layer. Access is enforced at three layers: network (self-referencing SG), IAM role policy (scoped per compute resource), and endpoint policy (blanket ceiling on allowed actions for all compute principals — Lambda, ECS, EC2).",
      "inputProperties": {
        "vpcId": {
          "type": "string",
          "description": "The ID of the VPC to create the endpoint in. Accepts both Anvil-managed VPC IDs and imported VPC IDs."
        },
        "privateSubnetIds": {
          "type": "array",
          "items": { "type": "string" },
          "description": "The IDs of the private subnets to attach the endpoint to. AWS places one ENI per subnet. Pass all private subnet IDs from your VPC — typically one per AZ."
        },
        "service": {
          "$ref": "#/types/anvil:aws:AwsVpcEndpointService",
          "description": "The AWS service to route privately. The full com.amazonaws.{region}.{service} name is constructed at deploy time from the resolved region — you never write it manually."
        },
        "overridePermissions": {
          "type": "array",
          "items": { "$ref": "#/types/anvil:aws:VpcEndpointPermission" },
          "description": "Explicit Allow and Deny permission statements for the endpoint policy. When omitted, the endpoint policy allows all actions (*) for all Anvil compute principals (Lambda, ECS, EC2). When set, only the declared actions are permitted — the caller is responsible for declaring every action their compute resources need. Supports both Allow and Deny effects. Resource defaults to \"*\" if omitted on a permission entry."
        }
      },
      "requiredInputs": ["vpcId", "privateSubnetIds", "service"],
      "properties": {
        "endpointId": {
          "type": "string",
          "description": "The ID of the VPC endpoint, e.g. vpce-0abc1234567890abc. Use this to reference the endpoint in IAM condition keys such as aws:SourceVpce."
        },
        "dnsName": {
          "type": "string",
          "description": "The first DNS name assigned to the endpoint, e.g. vpce-xxx.sqs.ap-southeast-2.vpce.amazonaws.com. With private DNS enabled, normal consumers use the standard AWS SDK hostname — this is exposed for debugging and multi-VPC architectures only."
        },
        "securityGroupId": {
          "type": "string",
          "description": "The ID of the dedicated security group attached to this endpoint. Uses a self-referencing ingress rule on port 443 — only compute resources with this SG explicitly attached can reach the endpoint at the network layer."
        }
      },
      "required": ["endpointId", "dnsName", "securityGroupId"]
    }
  },
  "types": {
    "anvil:aws:VpcEndpointPermission": {
      "type": "object",
      "description": "A single permission statement in the VPC endpoint policy. Effect must be Allow or Deny. Action is an IAM action string. Resource defaults to * if omitted.",
      "properties": {
        "effect": {
          "type": "string",
          "description": "The IAM effect — \"Allow\" or \"Deny\"."
        },
        "action": {
          "type": "string",
          "description": "The IAM action string, e.g. \"sqs:SendMessage\", \"secretsmanager:GetSecretValue\"."
        },
        "resource": {
          "type": "string",
          "description": "The ARN to scope this permission to. Defaults to \"*\" if omitted — applies to all resources of this service. Accepts Output<string> — pass resource ARNs directly, e.g. queue.arn, secret.arn."
        }
      },
      "required": ["effect", "action"]
    },
    "anvil:aws:AwsVpcEndpointService": {
      "type": "string",
      "description": "The AWS service to route privately via an Interface VPC Endpoint. Each value maps to the com.amazonaws.{region}.{suffix} endpoint service name.",
      "enum": [
        {
          "value": "ssm",
          "description": "AWS Systems Manager. Required alongside ssmmessages and ec2messages for full SSM functionality including Session Manager and Run Command."
        },
        {
          "value": "ssmmessages",
          "description": "SSM Session Manager messaging. Required alongside ssm and ec2messages."
        },
        {
          "value": "ec2messages",
          "description": "SSM Run Command messaging. Required alongside ssm and ssmmessages."
        },
        {
          "value": "secretsmanager",
          "description": "AWS Secrets Manager. Allows compute resources to call GetSecretValue and other Secrets Manager APIs without traversing the public internet."
        },
        {
          "value": "ecr.api",
          "description": "ECR control plane — authentication, image manifests, and repository metadata. Required alongside ecr.dkr for private image pulls."
        },
        {
          "value": "ecr.dkr",
          "description": "ECR data plane — image layer pulls. Required alongside ecr.api for private image pulls."
        },
        {
          "value": "sqs",
          "description": "Amazon Simple Queue Service. Covers all SQS operations including SendMessage, ReceiveMessage, and DeleteMessage — all are client-initiated HTTPS, one endpoint covers all operations."
        },
        {
          "value": "sns",
          "description": "Amazon Simple Notification Service. Allows compute resources to publish to SNS topics without traversing the public internet."
        },
        {
          "value": "lambda",
          "description": "AWS Lambda invoke. Allows private invocation of Lambda functions from within the VPC."
        },
        {
          "value": "logs",
          "description": "Amazon CloudWatch Logs. Required for compute resources in private subnets to ship logs to CloudWatch without a NAT Gateway."
        },
        {
          "value": "monitoring",
          "description": "Amazon CloudWatch Metrics. Required for compute resources in private subnets to publish custom metrics without a NAT Gateway."
        },
        {
          "value": "kms",
          "description": "AWS Key Management Service. Required for compute resources that perform envelope encryption, use KMS-managed secrets, or interact with services that call KMS on their behalf."
        },
        {
          "value": "sts",
          "description": "AWS Security Token Service. Required for IAM role assumption and temporary credential generation within private subnets."
        }
      ]
    }
  },
  "x-upstream-provider": "aws",
  "x-upstream-version": "v7.23.0"
}
